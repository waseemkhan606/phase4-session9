{
  "permissions": {
    "allow": [
      "Bash(curl -s -m 10 -X POST http://localhost:6006/graphql -H 'Content-Type: application/json' -d '{\"query\": \"{ projects { edges { node { name traces\\(first: 5\\) { edges { node { traceId spans { edges { node { name attributes } } } } } } } } } }\"}' -o /tmp/phoenix_traces.json)",
      "Bash(python3 -c ' *)"
    ]
  }
}
